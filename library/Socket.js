/** {Socket} Сервер для socket соединения @class
  *
  */
  export default class Socket {
  /** {Socket} @constructor
    * @param {object} config параметры
    */
    constructor(config) {
      this.config = config;
    }
  }
